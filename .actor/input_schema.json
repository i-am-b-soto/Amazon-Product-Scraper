{
    "title": "Amazon Product Scraper Made With Love",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "product_list_url": {
            "title": "Product list URL to start",
            "type": "string",
            "description": "Enter the URL of the Amazon Product List Page you want to start scraping",
            "editor": "textfield",
            "prefill": "https://www.amazon.com/s?k=cooker&page=2"
        },

        "use_resedential_proxies": {
            "title": "Use Resedential Proxies?",
            "type": "boolean",
            "description": "Use Resedential Proxies to perform this scrape? Decreases risk for bot detection but potentially increases cost.",
            "default": false
        },
        "add_human_behavior": {
            "title": "Add human-like behavior to requests made?",
            "type": "boolean",
            "description": "Add human-like behavior to requests made. Decreases risk for bot detection but risks the program running slower.",
            "default": true

        },
        "parallel_worker_count": {
            "title": "Parallel Worker Count",
            "type": "integer",
            "default": 3,
            "minimum": 1,
            "description": "Maximum number of concurrent requests (higher numbers require more cpu)"

        },
        "browser_context_count": {
            "title": "Browser Context Count",
            "type": "integer",
            "default": 3,
            "minimum": 1,
            "description": "Total number of browser context instances to store in memory (should not be lower than parallel worker count)"
        },
        "headless":{
            "title": "Run Headless",
            "type": "boolean",
            "default": false,
            "description": "Run the scraper headless? Or would you like a head?"
        },
        "request_timeout":{
            "title": "Global Request Timeout",
            "type": "integer",
            "default": 12000,
            "minimum": 3000,
            "description": "Time in ms to wait for each request before failing."
        }
    },  
    "required":["product_list_url"]
}
